{
  "name": "bookvault",
  "version": "1.0.0",
  "private": true,
  "description": "BookVault — Dark Fantasy Book Collection Platform",
  "scripts": {
    "vercel-build": "cd client && npm install && npm run build",
    "prisma-generate": "prisma generate",
    "vercel-postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^3.0.3",
    "google-auth-library": "^10.9.0",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@vercel/node": "^3.0.0",
    "prisma": "^5.22.0"
  }
}
